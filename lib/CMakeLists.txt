# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-lilacsat
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX
list(APPEND lilacsat_sources
    afsk/afsk.c
    afsk/ax25.c
    afsk/crc_ccitt.c
    afsk/fifobuf.c
    afsk/kfile.c
    afsk/wavegen.c
    afsk/direwolf_demod_afsk.c
    afsk/direwolf_dsp.c
    ccsds_afsk/ccsds_afsk.c
    ccsds/rs.c
    ccsds/ccsds.c
    ccsds/tab.c
    ccsds/metrics.c
    ccsds/randomizer.c
    ccsds/viterbi27.c
    kiss/kiss.c
    codec2/codebook.c
    codec2/codebookd.c
    codec2/codebookdt.c
    codec2/codebookge.c
    codec2/codebookjnd.c
    codec2/codebookjvm.c
    codec2/codebooklspmelvq.c
    codec2/codebookmel.c
    codec2/codebookres.c
    codec2/codebookvq.c
    codec2/codebookvqanssi.c
    codec2/codec2.c
    codec2/dump.c
    codec2/interp.c
    codec2/kiss_fft.c
    codec2/lpc.c
    codec2/lsp.c
    codec2/nlp.c
    codec2/pack.c
    codec2/phase.c
    codec2/postfilter.c
    codec2/quantise.c
    codec2/sine.c
    serial/serial.c
    afsk1200_tx_f_impl.cc
    afsk1200_rx_f_impl.cc
    angle_cf_impl.cc
    attach_preamble_and_tailer_impl.cc
    attach_rs_codeblock_impl.cc
    bpsk_demod_cb_impl.cc
    cc_decode_bb_impl.cc
    ccsds_afsk_decode_impl.cc
    ccsds_afsk_encode_impl.cc
    codec2_decode_bf_impl.cc
    codec2_encode_fb_impl.cc
    dslwp_tm_header_parser_impl.cc
    dslwp_tm_parser_impl.cc
    encode27_bb_impl.cc
    fec_decode_b_impl.cc
    fec_encode_b_impl.cc
    file_sink_pdu_impl.cc
    gmsk_demod_impl.cc
    kiss_decode_pdu_impl.cc
    kiss_encode_pdu_impl.cc
    lilacsat1_frame_depack_impl.cc
    pdu_f32_to_u8_impl.cc
    pipe_sink_pdu_impl.cc
    plan13/Plan13.cpp
    plan13_cc_impl.cc
    print_to_file_b_impl.cc
    rs_decode_pdu_impl.cc
    satellite_channel_cc_impl.cc
    send_file_pdu_impl.cc
    serial_sink_pdu_impl.cc
    sync_det_b_impl.cc
    two_bit_dpd_frame_recovery_impl.cc
    two_bit_dpd_precoder_impl.cc
    vitfilt27_bb_impl.cc
    vitfilt27_fb_impl.cc )

set(lilacsat_sources
    "${lilacsat_sources}"
    PARENT_SCOPE)
if(NOT lilacsat_sources)
    message(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT lilacsat_sources)

add_library(gnuradio-lilacsat SHARED ${lilacsat_sources})
target_link_libraries(gnuradio-lilacsat gnuradio::gnuradio-runtime)
target_include_directories(
    gnuradio-lilacsat
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
    PUBLIC $<INSTALL_INTERFACE:include>)
set_target_properties(gnuradio-lilacsat PROPERTIES DEFINE_SYMBOL "gnuradio_lilacsat_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-lilacsat PROPERTIES INSTALL_NAME_DIR
                                                    "${CMAKE_INSTALL_PREFIX}/lib")
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
gr_library_foo(gnuradio-lilacsat)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_lilacsat_sources)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-lilacsat)

if(NOT test_lilacsat_sources)
    message(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_lilacsat_sources)

foreach(qa_file ${test_lilacsat_sources})
    gr_add_cpp_test("lilacsat_${qa_file}" ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file})
endforeach(qa_file)
